# /sardar assets

- `og.png` — 1200×630 placeholder OG card, generated. Replace with a real render when there is one.
- `avatar.glb` — Ready Player Me head/bust (owner supplies). Absent → the page ships its procedural low-poly bust (step 3).

// src/app/sardar-ghaffar/seo.ts — constants shared by the page and the
// sitemap (a page.tsx may only export Next's reserved fields).

/** Bump when the page's content changes — feeds the sitemap `lastModified`
 *  and the ProfilePage `dateModified`. */
export const PAGE_LAST_MODIFIED = '2026-09-06'

// src/app/channelops/page.tsx — placeholder for the upcoming ChannelOps
// product page. Kept noindex until the real page ships.
import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'ChannelOps',
  robots: { index: false, follow: false },
}

export default function ChannelOpsPage() {
  return <h1>ChannelOps — coming soon</h1>
}
